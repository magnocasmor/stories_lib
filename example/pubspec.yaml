name: stories_lib_example
description: A new Flutter application.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.0.4
  image_picker: ^0.7.4
  cupertino_icons: ^1.0.0
  stories_lib:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
